summarise(CH = ifelse(any(CH == 10), 10, ifelse(any(CH == 1), 1, 0)),
C1 = ifelse(sum(C1) > 0, 1, 0),
C2 = ifelse(sum(C2) > 0, 1, 0),
C3 = ifelse(sum(C3) > 0, 1, 0),
C4 = ifelse(sum(C4) > 0, 1, 0),
C5 = ifelse(sum(C5) > 0, 1, 0))
final_rat = inner_join(cr_rat,rat_full,by="ID")
cell_counts = freq(rs)[,2]
final_rat = final_rat %>%
mutate(Value = 1:nrow(final_rat),
Count = cell_counts,
.after = ID) %>%
dplyr::select(-ID)
fid_lookup = read.csv("O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scripts_tools/FID_LOOKUP.csv")
shorts = fid_lookup$SHORTNAME[match(names(final_rat),fid_lookup$LONGNAME)] %>% discard(is.na(.))
names(final_rat) <- c("VALUE","COUNT","CH","C1","C2","C3","C4","C5",shorts)
rss = reclassify(rs,as.matrix(cbind(sort(ID),1:length(ID))),datatype="INT2U")
return(list(rss,final_rat))
})
rtn = lapply(1:2,function(x){
ID = unique(values(rs_list[[x]]))
rat = data.frame(ID) %>%
separate(ID, into = paste0("V",0:max(divs)), sep="", fill="left") %>%
mutate(across(everything(), ~ na_if(.x,""))) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0))) %>%
dplyr::select(-V0)
vals_lookup = setNames(list(c(1,4,6,9),
c(3,4,8,9),
c(5,6,8,9)),
1:3)
rats = lapply(1:3, function(x){
vals = vals_lookup[[x]] %>% as.character
rat_out = rat %>%
mutate(across(everything(), ~ ifelse(.x %in% vals, 1, 0)))
rat_out = rat_out[,(1+(ncol(rat_out) - divs[x])):ncol(rat_out)]
names(rat_out) = filter(lookup,set==x) %>% pull(combined_values) %>% rev
rat_out = cbind(ID=ID, rat_out)
rat_out
})
rat_full = rats %>% reduce(full_join, by = "ID")
add_cr = rat_full %>%
pivot_longer(2:42, names_to = "combined_values", values_to = "value")
cr_df = mutate(lookup, value=1) %>%
dplyr::select(value,combined_values,C1:C5) %>%
mutate(CH = rep(c(10,1),c(25,16)))
add_cr = left_join(add_cr,cr_df,by=c("value","combined_values")) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0)))
cr_rat = add_cr %>%
group_by(ID) %>%
summarise(CH = ifelse(any(CH == 10), 10, ifelse(any(CH == 1), 1, 0)),
C1 = ifelse(sum(C1) > 0, 1, 0),
C2 = ifelse(sum(C2) > 0, 1, 0),
C3 = ifelse(sum(C3) > 0, 1, 0),
C4 = ifelse(sum(C4) > 0, 1, 0),
C5 = ifelse(sum(C5) > 0, 1, 0))
final_rat = inner_join(cr_rat,rat_full,by="ID")
cell_counts = freq(rs_list[[x]])[,2]
final_rat = final_rat %>%
mutate(Value = 1:nrow(final_rat),
Count = cell_counts,
.after = ID) %>%
dplyr::select(-ID)
fid_lookup = read.csv("O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scripts_tools/FID_LOOKUP.csv")
shorts = fid_lookup$SHORTNAME[match(names(final_rat),fid_lookup$LONGNAME)] %>% discard(is.na(.))
names(final_rat) <- c("VALUE","COUNT","CH","C1","C2","C3","C4","C5",shorts)
rss = reclassify(rs_list[[x]],as.matrix(cbind(sort(ID),1:length(ID))),datatype="INT2U")
return(list(rss,final_rat))
})
str(rtn)
rtn[1]
rtn[1][1]
rtn[1][[1]]
rtn[[1]][[1]]
rtn[[1]]
#########################################################
#### CREATE DRILL DOWN CRITICAL HABITAT RASTER LAYER ####
#########################################################
# This script can be run once the polygon files have been exported from Google Earth Engine
# NOTE: needs > 18 GB of memory to run
# These scripts can be found:
# Potential: https://code.earthengine.google.com/?scriptPath=users%2Fcorinnaravilious%2FUNEP-WCMC_SharedScripts%3Ap08868_Critical_Habitat_Update%2FPotential_Critical_Habitat
# Likely: https://code.earthengine.google.com/?scriptPath=users%2Fcorinnaravilious%2FUNEP-WCMC_SharedScripts%3Ap08868_Critical_Habitat_Update%2FLikely_Critical_Habitat
# Install packages (if required)
if (!require("pacman")) install.packages("pacman")
pacman::p_load(sf,raster,fasterize,tidyverse,rgeos,terra,units,tictoc,foreign)
options(scipen = 999)
tic()
# set path variables (these will be the only lines that need changing in this script)
# path to where the GEE output shapefiles are stored
shapefile_path <- "O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scratch/"
# path to where you want the output saved
output_path <- "O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/outputs/"
# read in WGS shapefiles
likely_WGS <- st_read(paste0(shapefile_path, "Likely_Critical_Habitat_Polygon.shp")) %>%
mutate(Type="Likely")
potential_WGS <- st_read(paste0(shapefile_path, "Potential_Critical_Habitat_Polygon.shp")) %>%
mutate(Type="Potential")
# create Mollweide shps
# need to wrap dateline to avoid projection error
likely_moll <- st_wrap_dateline(likely_WGS,
options = c("WRAPDATELINE=TRUE","DATELINEOFFSET=90")) %>%
st_transform("ESRI:54009") %>%
st_cast("MULTIPOLYGON")
potential_moll <- st_wrap_dateline(potential_WGS,
options = c("WRAPDATELINE=TRUE","DATELINEOFFSET=90")) %>%
st_transform("ESRI:54009") %>%
st_cast("MULTIPOLYGON")
# Combine geometries of overlapping polygons - TO BE COMPLETED AND TESTED!
# combineGeoms(likely_moll, potential_moll, overlap=TRUE, boundary=TRUE, distance=TRUE,
#              append=TRUE, minover=0.1, maxdist=Inf, dissolve=TRUE, erase=TRUE)
# load example raster with appropriate extent (global) and resolution (1 km) properties (e.g. NatMod raster from the portal)
#raster_WGS <- raster('O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/raw_data/WCMC_natural_modified_habitat_screening_layer/natural_modified_habitat_screening_layer.tif')
raster_WGS = raster(res=1/12)
dataType(raster_WGS) <- "FLT8S"
raster_WGS
# create example raster in Mollweide with global extent and 1km resolution
raster_moll <- raster(crs=st_crs("ESRI:54009")$proj4string,
res=10000,
ext=extent(c(-18040095.7,18040095.7,-9020047.85,9020047.85)))
dataType(raster_moll) <- "FLT8S"
raster_moll
# combine to final
final_combined_WGS <- rbind(likely_WGS, potential_WGS) %>%
dplyr::select(Type,Feature,C1,C2,C3,C4,C5,Value)
final_combined_moll <- rbind(likely_moll, potential_moll) %>%
dplyr::select(Type,Feature,C1,C2,C3,C4,C5,Value)
final_combined_WGS$area <- st_area(final_combined_moll) %>% units::set_units(km2) %>% units::drop_units()
final_combined_moll$area <- st_area(final_combined_moll) %>% units::set_units(km2) %>% units::drop_units()
# create lookup table to assign values
combined_values <- unique(paste(final_combined_WGS$Type, final_combined_WGS$Feature, sep="; ")) %>%
sort
ntriggers = length(combined_values)
divs <- rep(ntriggers%/%3, 3)
mod <- ntriggers%%3
divs[seq_len(mod)] <- divs[seq_len(mod)] + 1
#values <- c(10^(1:divs[1]),3*(10^(1:divs[2])),5*(10^(1:divs[3])))
values <- c(1,10^(1:(divs[1]-1)),
3,3*(10^(1:(divs[2]-1))),
5,5*(10^(1:(divs[3]-1))))
set <- rep(1:3, divs)
lookup <- data.frame(combined_values, values, set) %>%
separate(combined_values, sep = "; ", into = c("Type","Feature"), remove = FALSE) %>%
left_join(st_drop_geometry(final_combined_WGS) %>% dplyr::select(Type:C5), by = c("Type","Feature"))
# join lookup table
final_combined_WGS <- final_combined_WGS %>%
inner_join(dplyr::select(lookup,-C1:-C5), by = c("Type","Feature"))
final_combined_moll <- final_combined_moll %>%
inner_join(dplyr::select(lookup,-C1:-C5), by = c("Type","Feature"))
drill_down_raster_moll_set1 <- fasterize(filter(final_combined_moll,set==1), raster_moll,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_moll_set2 <- fasterize(filter(final_combined_moll,set==2), raster_moll,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_moll_set3 <- fasterize(filter(final_combined_moll,set==3), raster_moll,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_WGS_set1 <- fasterize(filter(final_combined_WGS,set==1), raster_WGS,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_WGS_set2 <- fasterize(filter(final_combined_WGS,set==2), raster_WGS,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_WGS_set3 <- fasterize(filter(final_combined_WGS,set==3), raster_WGS,
field = "values",
fun = "sum",
background = 0)
stack_moll = stack(drill_down_raster_moll_set1,
drill_down_raster_moll_set2,
drill_down_raster_moll_set3)
stack_WGS = stack(drill_down_raster_WGS_set1,
drill_down_raster_WGS_set2,
drill_down_raster_WGS_set3)
rs_moll = raster::calc(stack_moll,sum,datatype="FLT8S")
rs_WGS = raster::calc(stack_WGS,sum,datatype="FLT8S")
rs_list = list(rs_moll,rs_WGS)
################################################################################
# RAT SETUP ####################################################################
################################################################################
#rat_cols <- setNames(1:41,rev(combined_values))
rtn = lapply(1:2,function(x){
ID = unique(values(rs_list[[x]]))
rat = data.frame(ID) %>%
separate(ID, into = paste0("V",0:max(divs)), sep="", fill="left") %>%
mutate(across(everything(), ~ na_if(.x,""))) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0))) %>%
dplyr::select(-V0)
vals_lookup = setNames(list(c(1,4,6,9),
c(3,4,8,9),
c(5,6,8,9)),
1:3)
rats = lapply(1:3, function(x){
vals = vals_lookup[[x]] %>% as.character
rat_out = rat %>%
mutate(across(everything(), ~ ifelse(.x %in% vals, 1, 0)))
rat_out = rat_out[,(1+(ncol(rat_out) - divs[x])):ncol(rat_out)]
names(rat_out) = filter(lookup,set==x) %>% pull(combined_values) %>% rev
rat_out = cbind(ID=ID, rat_out)
rat_out
})
rat_full = rats %>% reduce(full_join, by = "ID")
add_cr = rat_full %>%
pivot_longer(2:42, names_to = "combined_values", values_to = "value")
cr_df = mutate(lookup, value=1) %>%
dplyr::select(value,combined_values,C1:C5) %>%
mutate(CH = rep(c(10,1),c(25,16)))
add_cr = left_join(add_cr,cr_df,by=c("value","combined_values")) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0)))
cr_rat = add_cr %>%
group_by(ID) %>%
summarise(CH = ifelse(any(CH == 10), 10, ifelse(any(CH == 1), 1, 0)),
C1 = ifelse(sum(C1) > 0, 1, 0),
C2 = ifelse(sum(C2) > 0, 1, 0),
C3 = ifelse(sum(C3) > 0, 1, 0),
C4 = ifelse(sum(C4) > 0, 1, 0),
C5 = ifelse(sum(C5) > 0, 1, 0))
final_rat = inner_join(cr_rat,rat_full,by="ID")
cell_counts = freq(rs_list[[x]])[,2]
final_rat = final_rat %>%
mutate(Value = 1:nrow(final_rat),
Count = cell_counts,
.after = ID) %>%
dplyr::select(-ID)
fid_lookup = read.csv("O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scripts_tools/FID_LOOKUP.csv")
shorts = fid_lookup$SHORTNAME[match(names(final_rat),fid_lookup$LONGNAME)] %>% discard(is.na(.))
names(final_rat) <- c("VALUE","COUNT","CH","C1","C2","C3","C4","C5",shorts)
rss = reclassify(rs_list[[x]],as.matrix(cbind(sort(ID),1:length(ID))),datatype="INT2U")
return(list(rss,final_rat))
})
writeRaster(rtn[[1]][[1]],
filename = "C:\\Users\\sebastiandu\\OneDrive - WCMC\\Documents\\drill_down_moll.tif",
datatype = "INT2U")
foreign::write.dbf(as.data.frame(rtn[[1]][[2]]),
file = "C:\\Users\\sebastiandu\\OneDrive - WCMC\\Documents\\drill_down_moll.tif.vat.dbf")
writeRaster(rtn[[2]][[1]],
filename = "C:\\Users\\sebastiandu\\OneDrive - WCMC\\Documents\\drill_down_WGS.tif",
datatype = "INT2U")
foreign::write.dbf(as.data.frame(rtn[[2]][[2]]),
file = "C:\\Users\\sebastiandu\\OneDrive - WCMC\\Documents\\drill_down_WGS.tif.vat.dbf")
toc()
#########################################################
#### CREATE DRILL DOWN CRITICAL HABITAT RASTER LAYER ####
#########################################################
# This script can be run once the polygon files have been exported from Google Earth Engine
# NOTE: needs > 18 GB of memory to run
# These scripts can be found:
# Potential: https://code.earthengine.google.com/?scriptPath=users%2Fcorinnaravilious%2FUNEP-WCMC_SharedScripts%3Ap08868_Critical_Habitat_Update%2FPotential_Critical_Habitat
# Likely: https://code.earthengine.google.com/?scriptPath=users%2Fcorinnaravilious%2FUNEP-WCMC_SharedScripts%3Ap08868_Critical_Habitat_Update%2FLikely_Critical_Habitat
# Install packages (if required)
if (!require("pacman")) install.packages("pacman")
pacman::p_load(sf,raster,fasterize,tidyverse,rgeos,terra,units,tictoc,foreign)
options(scipen = 999)
tic()
# set path variables (these will be the only lines that need changing in this script)
# path to where the GEE output shapefiles are stored
shapefile_path <- "O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scratch/"
# path to where you want the output saved
output_path <- "O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/outputs/"
# read in WGS shapefiles
likely_WGS <- st_read(paste0(shapefile_path, "Likely_Critical_Habitat_Polygon.shp")) %>%
mutate(Type="Likely")
potential_WGS <- st_read(paste0(shapefile_path, "Potential_Critical_Habitat_Polygon.shp")) %>%
mutate(Type="Potential")
# create Mollweide shps
# need to wrap dateline to avoid projection error
likely_moll <- st_wrap_dateline(likely_WGS,
options = c("WRAPDATELINE=TRUE","DATELINEOFFSET=90")) %>%
st_transform("ESRI:54009") %>%
st_cast("MULTIPOLYGON")
potential_moll <- st_wrap_dateline(potential_WGS,
options = c("WRAPDATELINE=TRUE","DATELINEOFFSET=90")) %>%
st_transform("ESRI:54009") %>%
st_cast("MULTIPOLYGON")
# Combine geometries of overlapping polygons - TO BE COMPLETED AND TESTED!
# combineGeoms(likely_moll, potential_moll, overlap=TRUE, boundary=TRUE, distance=TRUE,
#              append=TRUE, minover=0.1, maxdist=Inf, dissolve=TRUE, erase=TRUE)
# load example raster with appropriate extent (global) and resolution (1 km) properties (e.g. NatMod raster from the portal)
raster_WGS <- raster('O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/raw_data/WCMC_natural_modified_habitat_screening_layer/natural_modified_habitat_screening_layer.tif')
dataType(raster_WGS) <- "FLT8S"
raster_WGS
# create example raster in Mollweide with global extent and 1km resolution
raster_moll <- raster(crs=st_crs("ESRI:54009")$proj4string,
res=1000,
ext=extent(c(-18040095.7,18040095.7,-9020047.85,9020047.85)))
dataType(raster_moll) <- "FLT8S"
raster_moll
# combine to final
final_combined_WGS <- rbind(likely_WGS, potential_WGS) %>%
dplyr::select(Type,Feature,C1,C2,C3,C4,C5,Value)
final_combined_moll <- rbind(likely_moll, potential_moll) %>%
dplyr::select(Type,Feature,C1,C2,C3,C4,C5,Value)
final_combined_WGS$area <- st_area(final_combined_moll) %>% units::set_units(km2) %>% units::drop_units()
final_combined_moll$area <- st_area(final_combined_moll) %>% units::set_units(km2) %>% units::drop_units()
# create lookup table to assign values
combined_values <- unique(paste(final_combined_WGS$Type, final_combined_WGS$Feature, sep="; ")) %>%
sort
ntriggers = length(combined_values)
divs <- rep(ntriggers%/%3, 3)
mod <- ntriggers%%3
divs[seq_len(mod)] <- divs[seq_len(mod)] + 1
#values <- c(10^(1:divs[1]),3*(10^(1:divs[2])),5*(10^(1:divs[3])))
values <- c(1,10^(1:(divs[1]-1)),
3,3*(10^(1:(divs[2]-1))),
5,5*(10^(1:(divs[3]-1))))
set <- rep(1:3, divs)
lookup <- data.frame(combined_values, values, set) %>%
separate(combined_values, sep = "; ", into = c("Type","Feature"), remove = FALSE) %>%
left_join(st_drop_geometry(final_combined_WGS) %>% dplyr::select(Type:C5), by = c("Type","Feature"))
# join lookup table
final_combined_WGS <- final_combined_WGS %>%
inner_join(dplyr::select(lookup,-C1:-C5), by = c("Type","Feature"))
final_combined_moll <- final_combined_moll %>%
inner_join(dplyr::select(lookup,-C1:-C5), by = c("Type","Feature"))
drill_down_raster_moll_set1 <- fasterize(filter(final_combined_moll,set==1), raster_moll,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_moll_set2 <- fasterize(filter(final_combined_moll,set==2), raster_moll,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_moll_set3 <- fasterize(filter(final_combined_moll,set==3), raster_moll,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_WGS_set1 <- fasterize(filter(final_combined_WGS,set==1), raster_WGS,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_WGS_set2 <- fasterize(filter(final_combined_WGS,set==2), raster_WGS,
field = "values",
fun = "sum",
background = 0)
drill_down_raster_WGS_set3 <- fasterize(filter(final_combined_WGS,set==3), raster_WGS,
field = "values",
fun = "sum",
background = 0)
stack_moll = stack(drill_down_raster_moll_set1,
drill_down_raster_moll_set2,
drill_down_raster_moll_set3)
stack_WGS = stack(drill_down_raster_WGS_set1,
drill_down_raster_WGS_set2,
drill_down_raster_WGS_set3)
rs_moll = raster::calc(stack_moll,sum,datatype="FLT8S")
rs_WGS = raster::calc(stack_WGS,sum,datatype="FLT8S")
rs_list = list(rs_moll,rs_WGS)
################################################################################
# RAT SETUP ####################################################################
################################################################################
#rat_cols <- setNames(1:41,rev(combined_values))
rtn = lapply(1:2,function(x){
ID = unique(values(rs_list[[x]]))
rat = data.frame(ID) %>%
separate(ID, into = paste0("V",0:max(divs)), sep="", fill="left") %>%
mutate(across(everything(), ~ na_if(.x,""))) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0))) %>%
dplyr::select(-V0)
vals_lookup = setNames(list(c(1,4,6,9),
c(3,4,8,9),
c(5,6,8,9)),
1:3)
rats = lapply(1:3, function(x){
vals = vals_lookup[[x]] %>% as.character
rat_out = rat %>%
mutate(across(everything(), ~ ifelse(.x %in% vals, 1, 0)))
rat_out = rat_out[,(1+(ncol(rat_out) - divs[x])):ncol(rat_out)]
names(rat_out) = filter(lookup,set==x) %>% pull(combined_values) %>% rev
rat_out = cbind(ID=ID, rat_out)
rat_out
})
rat_full = rats %>% reduce(full_join, by = "ID")
add_cr = rat_full %>%
pivot_longer(2:42, names_to = "combined_values", values_to = "value")
cr_df = mutate(lookup, value=1) %>%
dplyr::select(value,combined_values,C1:C5) %>%
mutate(CH = rep(c(10,1),c(25,16)))
add_cr = left_join(add_cr,cr_df,by=c("value","combined_values")) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0)))
cr_rat = add_cr %>%
group_by(ID) %>%
summarise(CH = ifelse(any(CH == 10), 10, ifelse(any(CH == 1), 1, 0)),
C1 = ifelse(sum(C1) > 0, 1, 0),
C2 = ifelse(sum(C2) > 0, 1, 0),
C3 = ifelse(sum(C3) > 0, 1, 0),
C4 = ifelse(sum(C4) > 0, 1, 0),
C5 = ifelse(sum(C5) > 0, 1, 0))
final_rat = inner_join(cr_rat,rat_full,by="ID")
cell_counts = freq(rs_list[[x]])[,2]
final_rat = final_rat %>%
mutate(Value = 1:nrow(final_rat),
Count = cell_counts,
.after = ID) %>%
dplyr::select(-ID)
fid_lookup = read.csv("O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scripts_tools/FID_LOOKUP.csv")
shorts = fid_lookup$SHORTNAME[match(names(final_rat),fid_lookup$LONGNAME)] %>% discard(is.na(.))
names(final_rat) <- c("VALUE","COUNT","CH","C1","C2","C3","C4","C5",shorts)
rss = reclassify(rs_list[[x]],as.matrix(cbind(sort(ID),1:length(ID))),datatype="INT2U")
return(list(rss,final_rat))
})
gc()
rtn = lapply(1:2,function(x){
ID = unique(values(rs_list[[x]]))
rat = data.frame(ID) %>%
separate(ID, into = paste0("V",0:max(divs)), sep="", fill="left") %>%
mutate(across(everything(), ~ na_if(.x,""))) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0))) %>%
dplyr::select(-V0)
vals_lookup = setNames(list(c(1,4,6,9),
c(3,4,8,9),
c(5,6,8,9)),
1:3)
rats = lapply(1:3, function(x){
vals = vals_lookup[[x]] %>% as.character
rat_out = rat %>%
mutate(across(everything(), ~ ifelse(.x %in% vals, 1, 0)))
rat_out = rat_out[,(1+(ncol(rat_out) - divs[x])):ncol(rat_out)]
names(rat_out) = filter(lookup,set==x) %>% pull(combined_values) %>% rev
rat_out = cbind(ID=ID, rat_out)
rat_out
})
rat_full = rats %>% reduce(full_join, by = "ID")
add_cr = rat_full %>%
pivot_longer(2:42, names_to = "combined_values", values_to = "value")
cr_df = mutate(lookup, value=1) %>%
dplyr::select(value,combined_values,C1:C5) %>%
mutate(CH = rep(c(10,1),c(25,16)))
add_cr = left_join(add_cr,cr_df,by=c("value","combined_values")) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0)))
cr_rat = add_cr %>%
group_by(ID) %>%
summarise(CH = ifelse(any(CH == 10), 10, ifelse(any(CH == 1), 1, 0)),
C1 = ifelse(sum(C1) > 0, 1, 0),
C2 = ifelse(sum(C2) > 0, 1, 0),
C3 = ifelse(sum(C3) > 0, 1, 0),
C4 = ifelse(sum(C4) > 0, 1, 0),
C5 = ifelse(sum(C5) > 0, 1, 0))
final_rat = inner_join(cr_rat,rat_full,by="ID")
cell_counts = freq(rs_list[[x]])[,2]
final_rat = final_rat %>%
mutate(Value = 1:nrow(final_rat),
Count = cell_counts,
.after = ID) %>%
dplyr::select(-ID)
fid_lookup = read.csv("O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scripts_tools/FID_LOOKUP.csv")
shorts = fid_lookup$SHORTNAME[match(names(final_rat),fid_lookup$LONGNAME)] %>% discard(is.na(.))
names(final_rat) <- c("VALUE","COUNT","CH","C1","C2","C3","C4","C5",shorts)
rss = reclassify(rs_list[[x]],as.matrix(cbind(sort(ID),1:length(ID))),datatype="INT2U")
return(list(rss,final_rat))
})
rm(stack_moll,stack_WGS,drill_down_raster_moll_set1,drill_down_raster_moll_set2,drill_down_raster_moll_set3)
rm(drill_down_raster_WGS_set1,drill_down_raster_WGS_set2,drill_down_raster_WGS_set3)
gc()
raster::removeTmpFiles()
rm(raster_WGS,raster_moll)
gc()
raster::removeTmpFiles()
rtn = lapply(1:2,function(x){
ID = unique(values(rs_list[[x]]))
rat = data.frame(ID) %>%
separate(ID, into = paste0("V",0:max(divs)), sep="", fill="left") %>%
mutate(across(everything(), ~ na_if(.x,""))) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0))) %>%
dplyr::select(-V0)
vals_lookup = setNames(list(c(1,4,6,9),
c(3,4,8,9),
c(5,6,8,9)),
1:3)
rats = lapply(1:3, function(x){
vals = vals_lookup[[x]] %>% as.character
rat_out = rat %>%
mutate(across(everything(), ~ ifelse(.x %in% vals, 1, 0)))
rat_out = rat_out[,(1+(ncol(rat_out) - divs[x])):ncol(rat_out)]
names(rat_out) = filter(lookup,set==x) %>% pull(combined_values) %>% rev
rat_out = cbind(ID=ID, rat_out)
rat_out
})
rat_full = rats %>% reduce(full_join, by = "ID")
add_cr = rat_full %>%
pivot_longer(2:42, names_to = "combined_values", values_to = "value")
cr_df = mutate(lookup, value=1) %>%
dplyr::select(value,combined_values,C1:C5) %>%
mutate(CH = rep(c(10,1),c(25,16)))
add_cr = left_join(add_cr,cr_df,by=c("value","combined_values")) %>%
mutate(across(everything(), ~ replace(.x, is.na(.x), 0)))
source("O:/f01_projects_active/Global/p08868_CriticalHabitatUpdate/scripts_tools/Create_Drill_Down_Critical_Habitat_Raster_Layer_Mollweide.R")
1210.53/60
